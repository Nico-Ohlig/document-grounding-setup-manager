---
applications:
  - name: document-grounding-setup-manager
    memory: 256M
    disk_quota: 256M
    instances: 1
    buildpacks:
      - python_buildpack
    command: gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 60
    env:
      FLASK_ENV: production
